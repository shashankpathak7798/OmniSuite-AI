import 'package:flutter/material.dart';

class TextModelMobileView extends StatelessWidget {
  const TextModelMobileView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}