import 'package:flutter/material.dart';

class TextGenerationMobileView extends StatelessWidget {
  const TextGenerationMobileView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}